
# pip freeze :

# This returns all the package installed in a given python environment along with the versions.


# pip freeze > requirements.txt  

# The above command creates a file named ‘requirements.txtʼ in the same directory containing the output of ‘pip freezeʼ.


# pip install -r requirements.txt

# Using the above command, We can distribute this file to other users, and they can recreate the same environment 