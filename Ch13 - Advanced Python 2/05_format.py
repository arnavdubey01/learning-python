# The format function was used earlier. Now, the efficient f string method is preffered.

a = "{0} is a good {1}".format("Harry", "boy")
print(a)

# Python 3.6 onwards, f string is mostly being preferred and used.